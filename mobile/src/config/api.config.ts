// API Configuration
// Mobile and admin MUST point at the same backend, otherwise rider applications
// submitted from the mobile app will not appear in the admin Rider Approval page
// (they would land in two separate databases).
export const API_BASE_URL = 'https://oneride-backend.onrender.com/api/v1';
